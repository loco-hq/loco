description: "A dataset that partitions data in the lake"
filePathTemplate: "${project}/datasets/${name}.yaml"
properties:
  project:
    type: slug
    segments: 2
    createOnly: true
  name:
    type: slug
    createOnly: true
  label:
    type: string
  description:
    type: string
