description: "A project defining a schema namespace"
filePathTemplate: "${project}/project.yaml"
properties:
  project:
    type: slug
    segments: 2
    createOnly: true
  label:
    type: string
  description:
    type: string
