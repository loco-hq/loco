version: 1
description: "A field belonging to a collection"
filePathTemplate: "${namespace}/versions/${version}/field/${collection}/${name}.yaml"
properties:
  name:
    type: string
  collection:
    type: string
  type:
    type: string
