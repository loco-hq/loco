# Build artifacts
target/

# Cargo.lock is typically committed for binaries but ignored for libraries.
# Since this is a workspace with both, we commit it — but noting for reference.

# Editor/IDE
*.swp
*.swo
*~
.idea/
.vscode/
*.iml

# OS files
.DS_Store
Thumbs.db

# Environment
.env

# SQLite database
*.db

# Auth session/user data (local dev)
auth/

# My personal notes
NOTES.md

# Local instance data — schema types are tracked, but instances are user-scoped
# scratch data. Test suites have their own fixtures under tests/suites/.
loco-apps/schemas/instances/
